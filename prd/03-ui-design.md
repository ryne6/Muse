# Muse UI 设计规范

**文档版本**: v1.0
**创建日期**: 2026-01-24
**设计风格**: 极简对话风格

---

## 1. 设计原则

### 1.1 核心设计哲学

**FOCUS** - 专注、极简、高效

- **Focused（专注）**: 界面元素聚焦核心功能，去除干扰
- **Organized（有序）**: 信息层次清晰，布局有规律
- **Clean（简洁）**: 干净的视觉，留白充足
- **User-centric（以用户为中心）**: 降低认知负担
- **Smooth（流畅）**: 过渡动画自然，交互响应快

### 1.2 设计关键词

- **极简** (Minimalist)
- **现代** (Modern)
- **专业** (Professional)
- **高效** (Efficient)
- **优雅** (Elegant)

### 1.3 参考产品

| 产品 | 借鉴点 |
|------|--------|
| **Claude.ai** | 对话界面设计、消息排版 |
| **Linear** | 现代审美、配色方案 |
| **Raycast** | 极简高效、搜索交互 |
| **VS Code** | 侧边栏布局、图标系统 |

---

## 2. 色彩系统

### 2.1 浅色模式 (Light Mode)

#### 主色调 (Primary)
```css
--primary-50:  #f5f3ff;   /* 背景色 */
--primary-100: #ede9fe;   /* Hover 背景 */
--primary-200: #ddd6fe;   /* 边框 */
--primary-300: #c4b5fd;
--primary-400: #a78bfa;
--primary-500: #8b5cf6;   /* 主强调色 */
--primary-600: #7c3aed;   /* 按钮、链接 */
--primary-700: #6d28d9;
--primary-800: #5b21b6;
--primary-900: #4c1d95;
```

#### 中性色 (Neutral)
```css
--gray-50:  #fafafa;      /* 背景 */
--gray-100: #f4f4f5;      /* 侧边栏背景 */
--gray-200: #e4e4e7;      /* 分割线、边框 */
--gray-300: #d4d4d8;
--gray-400: #a1a1aa;      /* 图标、次要文字 */
--gray-500: #71717a;
--gray-600: #52525b;
--gray-700: #3f3f46;
--gray-800: #27272a;      /* 主要文字 */
--gray-900: #18181b;      /* 标题 */
```

#### 语义色 (Semantic)
```css
/* 成功 */
--success-50:  #f0fdf4;
--success-500: #22c55e;
--success-700: #15803d;

/* 警告 */
--warning-50:  #fffbeb;
--warning-500: #f59e0b;
--warning-700: #b45309;

/* 错误 */
--error-50:  #fef2f2;
--error-500: #ef4444;
--error-700: #b91c1c;

/* 信息 */
--info-50:  #eff6ff;
--info-500: #3b82f6;
--info-700: #1d4ed8;
```

#### 应用场景
```css
/* 背景 */
--bg-primary: #ffffff;        /* 主背景 */
--bg-secondary: #fafafa;      /* 次级背景 */
--bg-sidebar: #f4f4f5;        /* 侧边栏 */
--bg-hover: #f4f4f5;          /* Hover 状态 */

/* 文字 */
--text-primary: #18181b;      /* 主要文字 */
--text-secondary: #71717a;    /* 次要文字 */
--text-tertiary: #a1a1aa;     /* 三级文字（提示等） */

/* 边框 */
--border-default: #e4e4e7;    /* 默认边框 */
--border-light: #f4f4f5;      /* 浅色边框 */

/* 强调 */
--accent: #8b5cf6;            /* 强调色 */
--accent-hover: #7c3aed;      /* 强调色 Hover */
```

---

### 2.2 深色模式 (Dark Mode)

#### 主色调 (Primary)
```css
--primary-50:  #1e1b2e;
--primary-100: #2d2640;
--primary-200: #3d3352;
--primary-300: #4d4064;
--primary-400: #6d5a8f;
--primary-500: #8b5cf6;   /* 主强调色 */
--primary-600: #a78bfa;
--primary-700: #c4b5fd;
--primary-800: #ddd6fe;
--primary-900: #ede9fe;
```

#### 中性色 (Neutral)
```css
--gray-50:  #09090b;      /* 最深背景 */
--gray-100: #18181b;      /* 主背景 */
--gray-200: #27272a;      /* 侧边栏背景 */
--gray-300: #3f3f46;      /* 卡片背景 */
--gray-400: #52525b;      /* 分割线 */
--gray-500: #71717a;      /* 次要文字 */
--gray-600: #a1a1aa;
--gray-700: #d4d4d8;      /* 主要文字 */
--gray-800: #e4e4e7;
--gray-900: #fafafa;      /* 标题 */
```

#### 应用场景
```css
/* 背景 */
--bg-primary: #18181b;        /* 主背景 */
--bg-secondary: #09090b;      /* 次级背景 */
--bg-sidebar: #27272a;        /* 侧边栏 */
--bg-hover: #3f3f46;          /* Hover 状态 */

/* 文字 */
--text-primary: #fafafa;      /* 主要文字 */
--text-secondary: #a1a1aa;    /* 次要文字 */
--text-tertiary: #71717a;     /* 三级文字 */

/* 边框 */
--border-default: #3f3f46;    /* 默认边框 */
--border-light: #27272a;      /* 浅色边框 */

/* 强调 */
--accent: #a78bfa;            /* 强调色 */
--accent-hover: #c4b5fd;      /* 强调色 Hover */
```

---

## 3. 字体系统

### 3.1 字体家族

#### 界面字体 (UI Font)
```css
--font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI",
             "Noto Sans", Helvetica, Arial, sans-serif;
```

**各平台字体**:
- **macOS**: `-apple-system` → SF Pro
- **Windows**: `"Segoe UI"`
- **Linux**: `"Noto Sans"`

#### 代码字体 (Code Font)
```css
--font-mono: "JetBrains Mono", "Fira Code", "Cascadia Code",
             "Source Code Pro", Menlo, Monaco, Consolas,
             "Courier New", monospace;
```

**推荐代码字体**:
1. **JetBrains Mono** (优先推荐，支持连字)
2. **Fira Code** (备选，连字支持)
3. **Cascadia Code** (Windows 推荐)

---

### 3.2 字体大小 (Font Size)

```css
--text-xs:   0.75rem;   /* 12px - 提示文字 */
--text-sm:   0.875rem;  /* 14px - 次要文字 */
--text-base: 1rem;      /* 16px - 正文 */
--text-lg:   1.125rem;  /* 18px - 小标题 */
--text-xl:   1.25rem;   /* 20px - 标题 */
--text-2xl:  1.5rem;    /* 24px - 大标题 */
--text-3xl:  1.875rem;  /* 30px - 页面标题 */
```

**应用场景**:
| 场景 | 字号 | 示例 |
|------|------|------|
| 页面大标题 | 2xl (24px) | "欢迎使用 Muse" |
| 会话标题 | xl (20px) | "新项目搭建" |
| 消息正文 | base (16px) | "帮我分析这个项目" |
| 次要信息 | sm (14px) | "2 分钟前" |
| 提示文字 | xs (12px) | "按 ⌘K 搜索" |

---

### 3.3 字重 (Font Weight)

```css
--font-light:    300;  /* 轻量 */
--font-normal:   400;  /* 常规 */
--font-medium:   500;  /* 中等（推荐标题） */
--font-semibold: 600;  /* 半粗 */
--font-bold:     700;  /* 粗体 */
```

**使用建议**:
- **标题**: `medium (500)` 或 `semibold (600)`
- **正文**: `normal (400)`
- **强调**: `semibold (600)`
- **避免使用**: `light (300)` 和 `bold (700)` (视觉不够现代)

---

### 3.4 行高 (Line Height)

```css
--leading-none:   1;      /* 标题 */
--leading-tight:  1.25;   /* 紧凑文本 */
--leading-snug:   1.375;
--leading-normal: 1.5;    /* 正文（推荐） */
--leading-relaxed: 1.625; /* 宽松文本 */
--leading-loose:  2;      /* 行间距大 */
```

**应用场景**:
- **Chat 消息**: `1.5` (舒适阅读)
- **代码块**: `1.6` (代码更清晰)
- **标题**: `1.25` (紧凑)

---

## 4. 间距系统 (Spacing)

### 4.1 间距标尺

```css
--spacing-0:  0;
--spacing-1:  0.25rem;  /* 4px */
--spacing-2:  0.5rem;   /* 8px */
--spacing-3:  0.75rem;  /* 12px */
--spacing-4:  1rem;     /* 16px - 基础单位 */
--spacing-5:  1.25rem;  /* 20px */
--spacing-6:  1.5rem;   /* 24px */
--spacing-8:  2rem;     /* 32px */
--spacing-10: 2.5rem;   /* 40px */
--spacing-12: 3rem;     /* 48px */
--spacing-16: 4rem;     /* 64px */
```

**使用原则**:
- 使用 **4 的倍数** (4px, 8px, 16px, 24px...)
- 基础单位: **16px** (--spacing-4)
- 避免使用奇数像素 (如 15px, 17px)

---

### 4.2 常用间距组合

| 场景 | 间距 | 示例 |
|------|------|------|
| 组件内边距 | 12px / 16px | 按钮、卡片 |
| 组件之间 | 16px / 24px | 会话列表项 |
| 段落间 | 16px | 消息之间 |
| 区块间 | 32px | 侧边栏和主内容区 |
| 页面边距 | 24px / 32px | 内容区到边缘 |

---

## 5. 圆角系统 (Border Radius)

```css
--radius-none: 0;
--radius-sm:   0.25rem;  /* 4px - 小元素 */
--radius-md:   0.375rem; /* 6px - 按钮、输入框 */
--radius-lg:   0.5rem;   /* 8px - 卡片 */
--radius-xl:   0.75rem;  /* 12px - 面板 */
--radius-2xl:  1rem;     /* 16px - 大容器 */
--radius-full: 9999px;   /* 圆形 */
```

**应用场景**:
| 元素 | 圆角 | 示例 |
|------|------|------|
| 按钮 | 6px (md) | 发送按钮 |
| 输入框 | 6px (md) | Chat 输入框 |
| 卡片 | 8px (lg) | 会话卡片 |
| 消息气泡 | 12px (xl) | AI 消息 |
| 头像 | full | 用户头像 |
| 代码块 | 8px (lg) | 代码展示 |

---

## 6. 阴影系统 (Shadows)

```css
/* 浅色模式阴影 */
--shadow-sm:  0 1px 2px 0 rgb(0 0 0 / 0.05);
--shadow-md:  0 4px 6px -1px rgb(0 0 0 / 0.1),
              0 2px 4px -2px rgb(0 0 0 / 0.1);
--shadow-lg:  0 10px 15px -3px rgb(0 0 0 / 0.1),
              0 4px 6px -4px rgb(0 0 0 / 0.1);
--shadow-xl:  0 20px 25px -5px rgb(0 0 0 / 0.1),
              0 8px 10px -6px rgb(0 0 0 / 0.1);

/* 深色模式阴影（更柔和） */
--shadow-sm-dark:  0 1px 2px 0 rgb(0 0 0 / 0.3);
--shadow-md-dark:  0 4px 6px -1px rgb(0 0 0 / 0.4),
                   0 2px 4px -2px rgb(0 0 0 / 0.3);
--shadow-lg-dark:  0 10px 15px -3px rgb(0 0 0 / 0.5),
                   0 4px 6px -4px rgb(0 0 0 / 0.4);
```

**使用原则**:
- 少用阴影，极简风格
- 仅在需要**层次感**时使用
- 深色模式阴影更深（增强对比）

**应用场景**:
| 元素 | 阴影 | 说明 |
|------|------|------|
| 按钮 | none | 极简风格不用阴影 |
| 悬浮卡片 | shadow-md | 鼠标悬停时 |
| 对话框 | shadow-lg | Modal 弹窗 |
| 下拉菜单 | shadow-md | Dropdown |
| 侧边栏 | none | 用边框分隔即可 |

---

## 7. 动画系统 (Animations)

### 7.1 过渡时长 (Duration)

```css
--duration-fast:   150ms;   /* 快速反馈 */
--duration-normal: 250ms;   /* 常规过渡 */
--duration-slow:   350ms;   /* 慢速过渡 */
```

**应用场景**:
- **Hover 状态**: 150ms (快速反馈)
- **展开/折叠**: 250ms (舒适)
- **页面切换**: 350ms (平滑)

---

### 7.2 缓动函数 (Easing)

```css
--ease-linear:     linear;
--ease-in:         cubic-bezier(0.4, 0, 1, 1);
--ease-out:        cubic-bezier(0, 0, 0.2, 1);
--ease-in-out:     cubic-bezier(0.4, 0, 0.2, 1);
--ease-bounce:     cubic-bezier(0.68, -0.55, 0.265, 1.55);
```

**推荐使用**:
- **默认**: `ease-out` (最自然)
- **Hover**: `ease-out`
- **Modal 弹出**: `ease-in-out`

---

### 7.3 常用动画

#### Fade In/Out
```css
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes fadeOut {
  from { opacity: 1; }
  to { opacity: 0; }
}
```

#### Slide In
```css
@keyframes slideInFromBottom {
  from {
    transform: translateY(10px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}
```

#### Scale
```css
@keyframes scaleIn {
  from {
    transform: scale(0.95);
    opacity: 0;
  }
  to {
    transform: scale(1);
    opacity: 1;
  }
}
```

---

## 8. 图标系统

### 8.1 图标库选择

**Lucide React** (推荐)
- ✅ 现代、简洁
- ✅ React 组件化
- ✅ 一致的设计语言
- ✅ shadcn/ui 默认使用

```bash
npm install lucide-react
```

```tsx
import { Send, Settings, Plus } from 'lucide-react'

<Send className="w-5 h-5" />
```

---

### 8.2 图标尺寸

```css
--icon-xs:  16px;  /* 小图标 */
--icon-sm:  20px;  /* 常规图标 */
--icon-md:  24px;  /* 中等图标 */
--icon-lg:  32px;  /* 大图标 */
--icon-xl:  48px;  /* 超大图标 */
```

**应用场景**:
| 场景 | 尺寸 | 示例 |
|------|------|------|
| 按钮图标 | 20px | 发送按钮 |
| 菜单图标 | 20px | 侧边栏 |
| 状态图标 | 16px | 加载状态 |
| 工具调用图标 | 24px | 工具展示 |
| 空状态图标 | 48px | 暂无数据 |

---

### 8.3 常用图标映射

| 功能 | 图标 | Lucide 名称 |
|------|------|------------|
| 发送消息 | ↗️ | `Send` |
| 新建会话 | ➕ | `Plus` |
| 设置 | ⚙️ | `Settings` |
| 搜索 | 🔍 | `Search` |
| 工作区 | 📁 | `FolderOpen` |
| 代码 | 💻 | `Code` |
| 文件 | 📄 | `File` |
| 运行 | ▶️ | `Play` |
| 停止 | ⏹️ | `Square` |
| 成功 | ✅ | `CheckCircle` |
| 错误 | ❌ | `XCircle` |
| 警告 | ⚠️ | `AlertTriangle` |
| 加载 | ⏳ | `Loader2` (带旋转) |

---

## 9. 布局系统

### 9.1 应用整体布局

```
┌─────────────────────────────────────────────────┐
│  Title Bar (30px) - macOS 集成标题栏             │
├───────────┬─────────────────────────────────────┤
│           │                                     │
│  Sidebar  │         Main Content Area           │
│  (240px)  │                                     │
│           │  - Chat 区域                        │
│  - 工作区  │  - Code Preview 区域                │
│  - 会话    │                                     │
│  - 设置    │                                     │
│           │                                     │
│  (固定宽)  │  (自适应宽度)                        │
│           │                                     │
└───────────┴─────────────────────────────────────┘
```

**尺寸规范**:
- **标题栏**: 30px (macOS 集成) / 40px (Windows/Linux)
- **侧边栏**: 240px (固定) / 可折叠为 60px (仅图标)
- **主内容区**: 剩余宽度，最小 600px
- **最小窗口**: 900px × 600px

---

### 9.2 侧边栏布局

```
┌──────────────┐
│  Logo (48px) │  ← App Logo
├──────────────┤
│              │
│  工作区列表   │  ← 高度自适应
│  📁 Project1 │
│  📁 Project2 │
│              │
├──────────────┤
│              │
│  会话列表     │  ← 高度自适应
│  💬 Chat 1   │
│  💬 Chat 2   │
│              │
├──────────────┤
│  ⚙️ 设置     │  ← 固定在底部
└──────────────┘
```

**间距规范**:
- 内边距: 12px
- 列表项间距: 4px
- 列表项高度: 36px
- 列表项圆角: 6px

---

### 9.3 Chat 区域布局

```
┌─────────────────────────────────────────┐
│  Chat Header (可选，面包屑导航)           │  60px
├─────────────────────────────────────────┤
│                                         │
│  消息区域 (Scrollable)                   │
│                                         │
│  👤 用户消息                             │  自适应高度
│                                         │
│  🤖 AI 消息                              │
│                                         │
│  [工具调用展示]                          │
│                                         │
│                                         │
├─────────────────────────────────────────┤
│  输入框区域                               │  最小 80px
│  ┌─────────────────────────────────┐   │  最大 200px
│  │ 💬 输入消息...                   │   │  (自动调整)
│  │                                 │   │
│  │                         [发送]  │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

**间距规范**:
- 消息间距: 16px
- 消息内边距: 16px
- 输入框边距: 16px
- 输入框圆角: 12px

---

## 10. 组件设计规范

### 10.1 按钮 (Button)

#### 尺寸
```tsx
// sm - 小按钮
height: 32px
padding: 0 12px
font-size: 14px

// md - 默认按钮
height: 40px
padding: 0 16px
font-size: 16px

// lg - 大按钮
height: 48px
padding: 0 24px
font-size: 18px
```

#### 变体 (Variants)
```tsx
// primary - 主要操作
bg: --accent
text: white
hover: --accent-hover

// secondary - 次要操作
bg: --bg-hover
text: --text-primary
border: --border-default

// ghost - 幽灵按钮
bg: transparent
text: --text-secondary
hover: --bg-hover

// danger - 危险操作
bg: --error-500
text: white
```

---

### 10.2 输入框 (Input)

```tsx
height: 40px
padding: 0 12px
border: 1px solid --border-default
border-radius: 6px
font-size: 16px

// Focus 状态
border: 2px solid --accent
outline: none

// Error 状态
border: 1px solid --error-500

// Disabled 状态
bg: --bg-secondary
cursor: not-allowed
opacity: 0.5
```

---

### 10.3 卡片 (Card)

```tsx
bg: --bg-primary
border: 1px solid --border-default
border-radius: 8px
padding: 16px

// Hover 状态
border: 1px solid --border-default
box-shadow: --shadow-md
```

---

### 10.4 消息气泡 (Message)

#### 用户消息
```tsx
align: right
bg: --accent
text: white
border-radius: 12px 12px 4px 12px  // 右下角小圆角
padding: 12px 16px
max-width: 70%
```

#### AI 消息
```tsx
align: left
bg: --bg-secondary
text: --text-primary
border-radius: 12px 12px 12px 4px  // 左下角小圆角
padding: 12px 16px
max-width: 70%
```

---

## 11. 响应式设计

### 11.1 断点 (Breakpoints)

```css
--screen-sm:  640px;   /* 手机横屏 */
--screen-md:  768px;   /* 平板 */
--screen-lg:  1024px;  /* 笔记本 */
--screen-xl:  1280px;  /* 桌面 */
--screen-2xl: 1536px;  /* 大屏 */
```

**桌面应用适配**:
- 最小窗口: 900px × 600px
- 推荐窗口: 1280px × 800px
- 侧边栏在 < 900px 时可折叠为图标栏

---

## 12. 可访问性 (Accessibility)

### 12.1 颜色对比度

**WCAG AA 标准**:
- 正文 (16px): 对比度 ≥ 4.5:1
- 大文本 (24px): 对比度 ≥ 3:1

**检查工具**:
- [WebAIM Contrast Checker](https://webaim.org/resources/contrastchecker/)

---

### 12.2 键盘导航

**必须支持**:
- `Tab` / `Shift+Tab`: 焦点切换
- `Enter`: 确认/提交
- `Esc`: 取消/关闭
- `⌘K` / `Ctrl+K`: 快速搜索
- `⌘N` / `Ctrl+N`: 新建会话

---

### 12.3 焦点样式

```css
/* 可见的焦点环 */
:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
```

---

## 13. 设计资源

### 13.1 Figma 设计文件

**待创建**:
- [ ] 色彩系统
- [ ] 组件库
- [ ] 页面原型

---

### 13.2 设计参考

**色彩灵感**:
- [Tailwind CSS Colors](https://tailwindcss.com/docs/customizing-colors)
- [shadcn/ui Themes](https://ui.shadcn.com/themes)

**布局参考**:
- [Claude.ai](https://claude.ai/)
- [Linear](https://linear.app/)
- [Raycast](https://www.raycast.com/)

---

**文档结束**
